## 2026-01-30 - Icon-Only Buttons Pattern
**Learning:** Icon-only buttons are used frequently in the Doctor Dashboard without accessible labels, relying solely on visual cues. This makes critical actions invisible to screen reader users.
**Action:** Systematically add `aria-label` and `title` attributes to all icon-only buttons during feature development to ensure WCAG compliance.
